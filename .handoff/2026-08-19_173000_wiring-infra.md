---
mode: pause
session_id: 20260819_173000_orchestrator
project: crawlkit
branch: master
head_commit: f6f819a
previous_handoff: none
ts: 2026-08-19T17:30:00Z
work: "Wiring/infra thread split out from session-accumulated work. crawlkit extraction, /go/ router, erotica-site templates, repo plumbing, cross-project migration."
---

# Handoff — 2026-08-19 17:30 UTC — Wiring/Infrastructure Thread

## TL;DR

- This thread owns **infrastructure**: crawlkit extraction (Phase 1 + Sprint 1 done), `/go/` router (shipped), erotica-site templates (shipped), repo plumbing, CI/CD, cross-project migration
- Companion thread: **smut generation** lives in `~/Projects/undercontent/.handoff/LATEST.md`
- **Phase 2 in progress.** Sprint 1 done (models + classifier). Sprints 2-4 pending. **Not blocked** by corpus crawl or matrix — can run independently.

## What "wiring/infrastructure" means in scope

| In scope | Out of scope (→ smut-gen handoff) |
|---|---|
| crawlkit extraction + tests | Persona bible writing |
| Phase 2 sprints 2/3/4 (storage, browser, adapters) | Story drafting |
| Cross-project migration (aquascrape, fish-atlas, etc.) | IP-Adapter image runs |
| `/go/` router + offers.json + shortcodes | Fanvue launch |
| erotica-site Hugo template updates | Caption library expansion |
| Repo shim plumbing + backwards compat | Voice rules enforcement |
| CI/CD for crawlkit | Reddit corpus crawl (currently running, but smut-side data) |
| PyPI release for crawlkit 0.2.0 | Persona bible synthesis |
| initiatives.yml registration | Matrix analysis |
| Netlify deploy plumbing | Real-person compliance application |

## Work Completed (this session)

### crawlkit extraction — Phase 1 + Sprint 1 done

| Commit | What |
|---|---|
| `f6f819a` | Sprint 1 docs (CHANGELOG entry + AGENTS.md status normalization) |
| `3c4a2b6` | Sprint 1 extraction: models.py + classifier.py |
| `614422d` | Phase 1: 8 utility modules + pyproject + AGENTS + README + .gitignore + Phase 2 plan + CHANGELOG |
| `b4b5145` | Initial Phase 1 commit |

**crawlkit shipped:** 10 modules, 10 tests, GitHub repo at https://github.com/wowthisiseasytoremember-stack/crawlkit

### ccarchive shims + tests
- `ccarchive` 8 modules replaced with re-export shims (textutil, urlutil, datetimeutil, metrics, logsetup, robots, health, pacing)
- `ccarchive` models.py + classifier.py replaced with shims (preserves ccarchive/taxonomy.json as default for `classify_story()`)
- All 7 ccarchive tests still pass after each extraction

### /go/ router — shipped (erotica-site)
- `data/offers.json` — 3 Luna offers (luna-fanvue, luna-commission, luna-x)
- `layouts/shortcodes/disclosure.html` — FTC affiliate disclosure (`type=affiliate|endorsement`)
- `layouts/shortcodes/goto.html` — `/go/<slug>?src=&pos=` server-routed shortcode
- `static/_redirects` — Netlify 302 rules
- `layouts/partials/author-card.html` — Luna Fanvue button routed through `/go/luna-fanvue`
- `layouts/_default/single.html` — added fanvueUrl CTA in story CTA section
- `content/authors/luna/_index.md` — restructured with `<disclosure type="endorsement">` + 3 `<goto>` shortcodes
- `content/stories/after-midnight-at-the-launch.md` — proof-of-concept footer
- All `/go/` links carry `rel="noopener nofollow sponsored ugc"` per FTC 16 CFR 255

### Registration
- `crawlkit` added to `~/plans/initiatives.yml` under `content-factory.reddit-research` family (alongside `reddit-monitor`, `ccarchive`)

## Work In Progress

- **No active in-flight work on this thread.** Sprints 2/3/4 are planned and ready to start.
- **Background:** Reddit corpus crawl (smut-side data source) — 13 rows so far, ~2-4 hrs remaining. Not blocking this thread.

## Next Move (when resumed)

### Sprint 2 — storage.py extraction (4-5 hrs)
- Move `ccarchive/storage.py` → `crawlkit/storage.py`
- Replace ccarchive shim
- Move `tests/test_storage.py` → `crawlkit/tests/test_storage.py`
- Add: multi-process concurrency test
- Verify: ccarchive 7 + crawlkit 13+ tests pass
- Commit + push both repos

### Sprint 3 — browser + probe + age_gates (4-5 hrs)
- Move `ccarchive/browser.py` → `crawlkit/browser.py`
- Move `ccarchive/probe.py` → `crawlkit/probe.py`
- **NEW:** `crawlkit/age_gates.py` — pluggable dispatcher (Reddit-specific handler stays in ccarchive)
- Add tests: CDP mock, selector probe against HTML strings, age-gate dispatch
- Verify: ccarchive scripts (ssc_crawl.py, fm_crawl.py, etc.) still work
- Commit + push both

### Sprint 4 — adapters/base.py + glue refactor (4-6 hrs)
- Move `ccarchive/adapters/base.py` → `crawlkit/adapters/base.py`
- Refactor all 6 site adapters to import from `crawlkit.adapters.base` directly
- Move + add tests
- Smoke test each site adapter via `ccarch probe --site <name>`
- Commit + push + tag `crawlkit v0.2.0`

### Phase 3 (post-extraction) — cross-project migration
- aquascrape (smallest siloed scraper) — first proof
- fish-atlas-scraper — second
- job-search — adopt browser.py
- shame-seo — adopt classifier.py

### erotica-site MVP-Mid (separate parked scope)
- **Blocked on Justin's design approval** (D1-D8 unresolved from prior handoff)
- `/handoff --read` for `erotica-site/.handoff/2026-08-19_120525_handoff.md` to see the parked state
- `/go/` router is the only thing shipped on this repo since the parking

## Open Questions

- **PyPI release for crawlkit 0.2.0:** need to set up trusted publisher (similar to ccarchive's PyPI config). Phase 4 of Phase 2 plan. Schedule for after Sprint 4.
- **aquascrape migration timing:** Sprint 2-4 first, or parallel? Aquascrape is small enough that it's a 5-10 hr independent sprint. Recommendation: **after Sprint 4** so the migrated project lands on a stable crawlkit 0.2.0.
- **Cross-project dependency:** crawlkit is registered under content-factory.reddit-research. As more projects adopt it, should it move to its own family? e.g., `content-factory.shared-primitives` already exists with `content-factory` repo. New family: `content-factory.crawl-primitives`? Defer until Phase 3 adoption happens.

## Files Touched (this thread)

- `~/Projects/crawlkit/` — entire new repo (pyproject, AGENTS, README, .gitignore, CHANGELOG, 10 modules, 3 test files, 1 plan doc)
- `~/Projects/ccarchive/ccarchive/{textutil,urlutil,datetimeutil,metrics,logsetup,robots,health,pacing,models,classifier}.py` — all shims
- `~/Projects/ccarchive/scripts/crawl_expansion.sh` — wrapper for 22-sub crawl (technically corpus-side but lives in ccarchive)
- `~/Projects/ccarchive/tests/test_classifier.py` — moved to crawlkit/tests/ (re-imports to crawlkit)
- `~/Projects/erotica-site/hugo-site/data/offers.json` — created
- `~/Projects/erotica-site/hugo-site/layouts/shortcodes/{disclosure,goto}.html` — created
- `~/Projects/erotica-site/hugo-site/static/_redirects` — created
- `~/Projects/erotica-site/hugo-site/layouts/partials/author-card.html` — modified
- `~/Projects/erotica-site/hugo-site/layouts/_default/single.html` — modified
- `~/Projects/erotica-site/hugo-site/content/authors/luna/_index.md` — restructured
- `~/Projects/erotica-site/hugo-site/content/stories/after-midnight-at-the-launch.md` — proof-of-concept
- `~/Projects/erotica-site/CHANGELOG.md` — entry appended
- `~/plans/initiatives.yml` — crawlkit added to reddit-research family

## Git Scope

| Repo | HEAD | Status |
|---|---|---|
| crawlkit | f6f819a | Clean. Sprint 1 done. |
| ccarchive | a1218a3 | Clean. Sprint 1 done. Phase 2 sprints 2-4 pending. |
| erotica-site | b4a9192 | Clean. `/go/` router live. MVP-Mid still parked. |
| agent-skills | 92c918a | Clean. MONETIZATION-GAPS-ROADMAP + REAL-PERSON-COMPLIANCE-DOCTRINE in place. |

## Resume Script

**If continuing Phase 2:**
1. `cd ~/Projects/crawlkit && git pull` (sync)
2. Read `~/Projects/crawlkit/docs/PHASE-2-EXTRACTION-PLAN.md` (Sprint 2 section)
3. Execute Sprint 2 (storage.py extraction): move file, shim ccarchive, move tests, add concurrency test, verify, commit + push

**If pivoting to Phase 3 (cross-project migration):**
1. Wait for Sprint 4 to land (crawlkit 0.2.0 tagged)
2. Pick first migration target (recommendation: aquascrape — smallest)
3. Follow migration guide (to be written after Phase 3 begins)

**If pivoting to erotica-site MVP-Mid:**
1. Read `~/Projects/erotica-site/.handoff/2026-08-19_120525_handoff.md`
2. Surface compact MVP-Mid design + D1-D8 to Justin
3. Get one-word "go" / list of overrides
4. On go: write spec → review → build

## See Also

- Smut generation thread: `~/Projects/undercontent/.handoff/LATEST.md`
- Phase 2 plan: `~/Projects/crawlkit/docs/PHASE-2-EXTRACTION-PLAN.md`
- reputation-mvp scraper extraction plan: `~/Projects/reputation-mvp/plans/scraper-library-extraction.md`
- Parked erotica-site MVP-Mid: `~/Projects/erotica-site/.handoff/2026-08-19_120525_handoff.md`
- ccarchive M1-3 complete: `~/Projects/ccarchive/.handoff/2026-08-19_162214_handoff.md`
- caption-primitives (precedent for "pieces meant to be ported" pattern): `~/Projects/caption-primitives/AGENTS.md`
